name = "localfs"
version = "0.1.0"
namespace = "LOCALFS"
summary = "Local filesystem resource plugin"
description = "Local filesystem plugin for formae — manages directories and files on disk"
category = "other"
license = "Apache-2.0"
minFormaeVersion = "0.84.0"
output { renderer = new JsonRenderer {} }
